// ── AdSense の広告枠定義 ────────────────────────────────────────
//
// 2026-09-11に審査通過（3回の不承認を経て。[[adsense-recovery]]）。
//
// スロットIDは AdSense管理画面「広告 > 広告ユニットごと」で作ったユニットの
// data-ad-slot（10桁の数字）。**空文字の間 AdUnit は何も描画しない**ので、
// このファイルにIDを入れた瞬間だけ広告が出る。先に枠だけ入れて壊れないのは
// そのため（IDが無いのに <ins> を出すと空箱のぶんレイアウトが崩れる）。
//
// 自動広告（Auto ads）は使わない。商品グリッド・レシート風モーダル・
// 横スクロールのタブと、自動挿入が最も事故を起こす形が揃っているうえ、
// どこに何が出たか後から追えない。枠はここで固定して持つ。
export const AD_CLIENT = "ca-pub-1657546819928079";

export const AD_SLOTS = {
  // 商品ページ本文内（楽天導線の下・関連ガチャの上）。ディスプレイ/レスポンシブ
  inContent: "6221916072",
  // 商品グリッドの行と行の間に横一列で挟む帯（インフィード）。
  // トップ・ブランド・キャラ・シリーズ・発売月で共通。グリッドより上には
  // 置かない（ファーストビューは商品を出す方針）。掲載が数件しかなくて
  // 挟む場所が無いページでは、グリッドの直下に落ちる
  inFeed: "7534997742",
  // ページ末尾、フッターの直前。ディスプレイ/レスポンシブ。
  // 商品ページとブログ専用（一覧は multiplex が末尾を持つ）
  pageBottom: "8918668504",
  // 一覧ページの終わり、「〜から探す」導線の手前。Multiplex（autorelaxed）。
  // このサイトは元々カードのグリッドなので、グリッド型のネイティブ広告が
  // 構造的に馴染む。一覧を見終わって次を探している文脈にも合う。
  // ラベル（スポンサーリンク）はGoogleが枠内に描くので、こちらでは出さない
  multiplex: "7992951296",
  // ブログ記事の途中。AdSenseの「記事内広告」ユニット（fluid）
  inArticle: "7830511219",
};

export function adSlot(name) {
  return AD_SLOTS[name] || "";
}
